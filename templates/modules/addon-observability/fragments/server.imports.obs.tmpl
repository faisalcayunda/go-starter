// region:imports (addon-observability) — paket observability di-import (alias obs)
// agar endpoint /metrics Prometheus dapat di-wire ke router server.
obs "{{ modJoin .ModulePath "internal/platform/observability" }}"
